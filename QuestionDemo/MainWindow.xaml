<Window x:Class="QuestionDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuestionDemo"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel, IsDesignTimeCreatable=False}"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Demo App" Height="450" Width="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition />
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Menu>
      <MenuItem Header="_File">
        <MenuItem Header="_Clear Count" Command="{Binding ClearCountCommand}"/>
        <Separator />
        <MenuItem Header="E_xit" Command="{x:Static ApplicationCommands.Close}"/>
      </MenuItem>
    </Menu>
    
    <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
      <Image />
      <TextBlock Text="{Binding Count, StringFormat='{}{0} Clicks'}" Margin="0,10"/>
      <ComboBox Width="200" Margin="0,10"
                ItemsSource="{Binding Options}"
                SelectedItem="{Binding SelectedOption, Mode=TwoWay}" />
      <Button Margin="10" Content="_Click Me" Command="{Binding IncrementCountCommand}" />

      <materialDesign:Badged Margin="5"
                       Badge="{Binding SpecialCommandBadge}"
                       ToolTip="{Binding SpecialCommandTooltip}"
                       ToolTipService.InitialShowDelay="0"
                       ToolTipService.Placement="MousePoint">
        <Button Margin="2"
          Command="{Binding SendSpecialCommand}"
          Content="Button 1"
          Style="{StaticResource MaterialDesignOutlinedLightButton}" />
      </materialDesign:Badged>
      <materialDesign:Badged Margin="5"
                       Badge="{Binding SpecialCommandBadge}"
                       ToolTip="{Binding SpecialCommandTooltip}"
                       ToolTipService.InitialShowDelay="0"
                       ToolTipService.Placement="MousePoint">
        <Button Margin="2"
          Command="{Binding SendSpecialCommand}"
          Content="Button 2"
          Style="{StaticResource MaterialDesignOutlinedLightButton}" />
      </materialDesign:Badged>
      <materialDesign:Badged Margin="5"
                 Badge="{Binding SpecialCommandBadge}"
                 ToolTip="{Binding SpecialCommandTooltip}"
                 ToolTipService.InitialShowDelay="0"
                 ToolTipService.Placement="MousePoint">
        <Button Margin="2"
    Command="{Binding SendSpecialCommand}"
    Content="Button 3"
    Style="{StaticResource MaterialDesignOutlinedLightButton}" />
      </materialDesign:Badged>
    </StackPanel>
  </Grid>
</Window>
